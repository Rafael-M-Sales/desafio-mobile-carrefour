/**
 * Classe principal para conter elementos e métodos comuns
 */
class Page {
    /**
     * Abre uma sub-página do app (se aplicável)
     */
    async open(path) {
        // No contexto mobile, o 'open' geralmente é a inicialização do app
        // mas podemos usar para navegação profunda se suportado
    }
}
module.exports = Page;
